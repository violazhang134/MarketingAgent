# 快速上手指南 🚀

> 本文档帮助你在 5 分钟内启动 Marketing Agent 项目，适合人类开发者和 LLM 代理阅读。

---

## 环境要求

| 依赖       | 最低版本 | 推荐版本 |
| ---------- | -------- | -------- |
| Node.js    | 18.x     | 20.x+    |
| npm / pnpm | 8.x      | 最新     |

> **提示**：本项目使用 React 19 和 Next.js 16，请确保 Node.js 版本兼容。

---

## 一键启动

```bash
# 1. 进入 web 目录
cd MarketingAgent/web

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev
```

启动后访问 [http://localhost:3000](http://localhost:3000) 即可看到应用。

---

## 项目结构速览

```
MarketingAgent/
├── prdV1.md                    # 产品需求文档
├── web/                         # 主应用
│   ├── app/                     # Next.js 页面路由
│   │   ├── page.tsx             # 首页（控制塔）
│   │   ├── agent/               # Agent 自动化模式
│   │   ├── canvas/              # 无限画布页面
│   │   ├── feed/                # 决策流
│   │   └── launch/              # 发布包生成
│   ├── components/              # UI 组件
│   │   ├── canvas/              # 画布组件族
│   │   ├── ui/                  # 原子组件库
│   │   └── report/              # 报告组件
│   ├── lib/                     # 核心逻辑
│   │   ├── stores/              # Zustand 状态管理
│   │   ├── engines/             # 业务计算引擎
│   │   └── types/               # TypeScript 类型定义
│   └── docs/                    # 文档目录（你在这里）
```

---

## 常用命令

| 命令            | 说明                     |
| --------------- | ------------------------ |
| `npm run dev`   | 启动开发服务器（热重载） |
| `npm run build` | 构建生产版本             |
| `npm run start` | 启动生产服务器           |
| `npm run lint`  | 运行 ESLint 检查         |

---

## 开发工具推荐

- **VS Code 扩展**：

  - ESLint
  - TypeScript Vue Plugin (Volar) - 用于 TSX 高亮
  - Tailwind CSS IntelliSense
  - Error Lens - 行内错误提示

- **浏览器扩展**：
  - React Developer Tools

---

## 常见问题 FAQ

### Q: 启动时报错 `Module not found`？

```bash
rm -rf node_modules package-lock.json
npm install
```

### Q: 端口 3000 被占用？

```bash
# 查找并终止占用进程
lsof -i :3000
kill -9 <PID>

# 或使用其他端口
npm run dev -- -p 3001
```

### Q: TypeScript 类型报错但项目能运行？

这可能是 IDE 缓存问题，尝试：

1. 重启 TypeScript 服务（VS Code: `Cmd+Shift+P` → `TypeScript: Restart TS Server`）
2. 删除 `.next` 目录后重启

---

## 下一步

- 📖 阅读 [架构文档](../ARCHITECTURE.md) 了解系统设计
- 🎨 阅读 [UI 指南](./UI_GUIDE.md) 了解视觉规范
- 🔧 阅读 [开发指南](./DEV_GUIDE.md) 了解编码规范
- 📦 阅读 [状态管理 API](./STORES_API.md) 了解 Store 用法

---

> "最好的文档让人一看就会，不看也能猜对。"
